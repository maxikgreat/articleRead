
const initialState = {
    token: null,
    error: null,
    isLoading: false
}

export default function userReducer(state = initialState, {type, payload}){
    switch (type) {
        default:
            return state
    }
}