
const initialState = {
    uId: null,
    token: null,
    error: null,
    isLoading: false,
    data: null
}

export default function userReducer(state = initialState, {type, payload}){
    switch (type) {
        default:
            return state
    }
}