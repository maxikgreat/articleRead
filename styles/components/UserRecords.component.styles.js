import {StyleSheet} from 'react-native'

export default StyleSheet.create({
    container:{
        flex:1,
        justifyContent: "flex-start",
        alignItems: 'center'
    },
    recordsContainer: {
        borderWidth: 1,
        borderColor: 'black',
        width: 300
    },
    recordHld: {
        width: 200
    },
    recordTitle: {
        fontSize: 30
    }
})